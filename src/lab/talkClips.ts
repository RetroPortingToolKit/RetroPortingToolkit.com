// Slugs that have a recorded motion clip at /previews/<slug>.{mp4,webp}.
// Cards in this set render the clip instead of a static cover. Add your own
// slugs once the clips exist; an empty set means every card stays static.
export const TALK_CLIPS = new Set<string>([]);
