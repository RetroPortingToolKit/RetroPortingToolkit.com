/** Public documentation for the browser-agent tools. Kept dependency-free so
    ordinary UI links do not import the tool handlers and their search stack. */
export const SITE_TOOLS_SLUG = "reference/site-tools";
export const SITE_TOOLS_PATH = `/docs/${SITE_TOOLS_SLUG}`;
